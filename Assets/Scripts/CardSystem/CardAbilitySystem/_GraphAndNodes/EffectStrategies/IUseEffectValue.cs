namespace CardSystem
{
    //simple identifier class for easy editor scripts
    public interface IUseEffectValue
    {
        /*protected virtual int GetUpgradeEffectIncrease()
        {
            if (this is not EffectStrategy) return 0;
            var def = (this as EffectStrategy).graph as CardAbilityDefinition;

            
        }*/
    }
}